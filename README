websocket-videostream
=====================

Allows to stream output of ffmpeg process to browser using websocket and MJPEG.


Install and run
---------------

Requires node.js.

$ make deploy
$ make run

